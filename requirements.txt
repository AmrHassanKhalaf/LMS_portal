# =============================================================
# Student Management System — Python Dependencies
# =============================================================
# Install with: pip install -r requirements.txt
# =============================================================

# Core Framework
Django>=5.1,<6.0

# Image handling for student photo uploads
Pillow>=10.0

# Beautiful, DRY Django forms with Bootstrap 5
django-crispy-forms>=2.0
crispy-bootstrap5>=2024.2

# Environment variable management — keeps secrets out of code
python-decouple>=3.8

# Production WSGI server (used on deployment)
gunicorn>=22.0

# Serve static files efficiently in production
whitenoise>=6.5

# API
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
